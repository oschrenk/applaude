// Package internal holds the hook's core logic: settings loading, compound
// command parsing (via the shfmt syntax library), allow/deny prefix matching,
// and the PreToolUse hook decision + I/O.
package internal
